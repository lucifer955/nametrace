{
	"name": "nametrace",
	"version": "0.1.0",
	"private": true,
	"description": "Static name availability checker for OSS projects.",
	"scripts": {
		"start": "live-server --open=src/index.html --port=5173"
	},
	"devDependencies": {
		"live-server": "^1.2.2"
	}
}
